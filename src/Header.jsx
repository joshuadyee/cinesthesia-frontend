export function Header() {
  return (
    <header>
      <a href="/">Home</a> || <a href="/signup">Create Account</a> || <a href="/login">Login</a> || <a href="/logout">Log Out</a> || <a href="/users/index">Users</a> || <a href="/films/index">Films</a>
    </header>
  )
}